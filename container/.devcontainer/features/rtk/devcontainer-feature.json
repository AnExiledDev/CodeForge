{
	"id": "rtk",
	"version": "1.0.0",
	"name": "RTK (Rust Token Killer)",
	"description": "Installs RTK CLI proxy that compresses command output before it reaches LLM context windows (60-90% token savings)",
	"documentationURL": "https://github.com/rtk-ai/rtk",
	"options": {
		"version": {
			"type": "string",
			"description": "Version to install: 'latest' or a specific semver. Use 'none' to skip.",
			"default": "latest"
		},
		"username": {
			"type": "string",
			"description": "Container user to install for",
			"default": "automatic"
		}
	},
	"installsAfter": ["ghcr.io/devcontainers/features/node"]
}
