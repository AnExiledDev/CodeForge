{
	"id": "oh-my-claude",
	"version": "1.0.0",
	"name": "oh-my-claude",
	"description": "Multi-provider proxy for Claude Code with Chinese LLM routing",
	"documentationURL": "https://github.com/lgcyaxi/oh-my-claude",
	"options": {
		"version": {
			"type": "string",
			"default": "latest",
			"description": "oh-my-claude version to install (latest, semver, or none to skip)"
		},
		"shells": {
			"type": "string",
			"default": "both",
			"enum": ["bash", "zsh", "both"],
			"description": "Which shells to clean up legacy OMC blocks from"
		},
		"username": {
			"type": "string",
			"default": "automatic",
			"description": "Container user to install for"
		},
		"providerAgentsOnly": {
			"type": "boolean",
			"default": true,
			"description": "Keep provider agents and remove generated role agents that overlap CodeForge agents"
		}
	},
	"installsAfter": ["ghcr.io/devcontainers/features/node:1"]
}
