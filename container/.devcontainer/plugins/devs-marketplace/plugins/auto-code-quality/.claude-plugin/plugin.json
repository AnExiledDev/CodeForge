{
	"name": "auto-code-quality",
	"description": "Code quality plugin: collects edited files on edit, validates JSON/JSONC/YAML/TOML syntax instantly, tracks background activity (subagents + background bash), and gates stops with a /cq skill prompt — format, lint, and test on demand instead of automatically",
	"author": {
		"name": "AnExiledDev"
	}
}
